apiVersion: v1
kind: Service
metadata:
  name: taskmanager-service
spec:
  type: NodePort
  selector:
    app: taskmanager
  ports:
    - protocol: TCP
      port: 8080        # intern port cluster
      targetPort: 8080  # springboot app port
      nodePort: 30080   # accesible browser port